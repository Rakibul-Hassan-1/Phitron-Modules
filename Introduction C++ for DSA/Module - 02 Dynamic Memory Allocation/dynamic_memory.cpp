#include <bits/stdc++.h>
using namespace std;
signed main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int *ptr = new int;
    //  এখানে new int  ব্যাবহার করে একটি হিপ মেমরি তৈরি করা হয়েছে!
    *ptr = 20;
    cout << *ptr << endl; // Dereffaracne  করে ভালু প্রিন্ট করা হয়ছে!
    
}
