#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    cin >> n;
    vector<int> v2(n);
    for (int i = 0; i < v2.size(); i++)
    {
        cin >> v2[i];
    }
    vector<int> v(v2);
    for (int i = 0; i < v.size(); i++)
    {
        cin >> v[i];
    }

 
    for (int i = 0; i < ans.size(); i++)
    {
        cout << ans[i] << " ";
    }

    return 0;
}