#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b, c;
    cin >> a >> b >> c;
    if (a > 0 && a == b && b == c)
        cout << "Yes";
    else
        cout << "No";

    return 0;
}